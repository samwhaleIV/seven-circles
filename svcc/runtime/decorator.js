function Decorator(layerBridge) {
    this.stamp = ({
        attempts = 100,
        qualifier = () => true,
        stamp = {}
    }) => {
        const ownStamp = Object.assign({},stamp);

    }
}
export default Decorator;
